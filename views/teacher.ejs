<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Teacher</title>
  </head>
  <body>
    <h1>Teacher</h1>
    <a href="/">Back</a>
    <a href="/teacher/add">ADD teacher</a>
</form>
    <table border="3px">
      <tr>
        <th>Id</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Email</th>
        <th>Subject</th>
        <th>Action</th>
      </tr>
      <%data.forEach(d=>{%>
        <tr>
          <td><%=d.id%></td>
          <td><%=d.first_name%></td>
          <td><%=d.last_name%></td>
          <td><%=d.email%></td>
          <td>
            <% if(d.SubjectId == null){ %>
            <td>Unassigned</td>
            <% } else { %>
              <%= d.Subject.subject_name %>
            <% } %>
          </td>
            <td><a href="/teacher/edit/<%=d.id%>">Edit</a>
            <a href="/teacher/delete/<%=d.id%>">Delete</a></td>
        </tr>
      <%})%>
    </table>
  </body>
</html>
